{
  "app_id": "web-tools",
  "version": "1.4.0",
  "capabilities": ["store:read", "store:write"],
  "tools": [
    {
      "name": "tool_web_tools_chat",
      "description": "Web diagnostics — DNS, SSL, WHOIS, HTTP headers grade, email SPF DMARC DKIM, blacklist 30 DNSBL, port scan, SMTP test, geo probe EU/US/SG/MD, full parallel domain audit, domain health monitors with groups and check profiles",
      "scopes": ["*"],
      "parameters": {
        "message": {"type": "string", "required": false},
        "kwargs":  {"type": "string", "required": true}
      }
    },
    {
      "name": "skeleton_refresh_web_tools",
      "description": "Background refresh: domain monitor statuses from last scan snapshots.",
      "scopes": [],
      "parameters": {}
    },
    {
      "name": "__panel__sidebar",
      "description": "Panel left: Domain Scan and IP Scan (tab buttons). TagInput domains, toggle checks, expandable results per domain/IP with rich detail.",
      "scopes": [],
      "parameters": {
        "params": {"type": "string", "required": true}
      }
    },
    {
      "name": "__panel__overview",
      "description": "Panel right: monitors — stats, bar chart, sorted cards. view=monitors (default) or view=new (New Monitor form with toggles).",
      "scopes": [],
      "parameters": {
        "params": {"type": "string", "required": true}
      }
    },
    {
      "name": "__panel__detail",
      "description": "Panel right: monitor detail — pie chart, expandable domain list (per-check KV), Settings accordion.",
      "scopes": [],
      "parameters": {
        "params": {"type": "string", "required": true}
      }
    }
  ],
  "signals": [],
  "schedules": [
    {
      "name": "wt_monitor_runner",
      "cron": "0 * * * *",
      "description": "Hourly: runs overdue domain health monitors across all users"
    }
  ],
  "required_scopes": ["store:read", "store:write"]
}
